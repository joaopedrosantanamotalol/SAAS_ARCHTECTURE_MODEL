# Este é um repositório base para a criação de SaaS baseado em branches de Frontend / API / Containers

# O Front e o backend não devem permanecer no mesmo folder, a estrutura do projeto vai potencialmente se basear em um Conatiner Docker que vai ser hospedado, O Container vai possuir uma imagem de ambiente leve para priorizar o desempenho da API.
